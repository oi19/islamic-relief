import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  container: {
    // position: "absolute",
    // bottom: getHeight(61),
    // right: getWidth(6),
  },
  button: {marginLeft: 0},
});
export default styles;
